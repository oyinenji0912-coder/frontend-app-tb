# Frontend React Vite Portal TB

Jalankan backend NestJS terlebih dahulu, lalu:

```bash
npm run dev
```

Buka `http://localhost:5173`. Vite meneruskan permintaan `/api` ke backend pada `http://127.0.0.1:8766`.
